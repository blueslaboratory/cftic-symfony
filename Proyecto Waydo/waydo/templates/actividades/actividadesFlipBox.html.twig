<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actividades</title>
    <style>
        * {
            border: 2px solid blue;
        }

        body {
            margin: 0 10%;
        }

        h1 {
            text-decoration: underline;
            text-align: center;
            font-size: 72px;
            font-family: sans-serif monospace;
            font-weight: 100;
        }

        .distrito {
            text-align: center;
        }

        .actividades {
            font-family: Arial, Helvetica, sans-serif;
            display: inline-flex;
            flex-wrap: wrap;
            justify-content: center;
            width: 100%;
        }

        .flip-box {
            box-sizing: border-box;
            justify-content: center;
            background-color: transparent;
            width: 400px;
            height: 300px;
            margin: 15px;
            /* border: 1px solid #f1f1f1; */
            perspective: 1000px;
        }

        .flip-box-inner {
            position: relative;
            width: 100%;
            height: 100%;
            text-align: center;
            transition: transform 0.8s;
            transform-style: preserve-3d;
        }

        .flip-box:hover .flip-box-inner {
            transform: rotateY(180deg);
        }

        .flip-box-front,
        .flip-box-back {
            position: absolute;
            width: 100%;
            height: 100%;
            -webkit-backface-visibility: hidden;
            backface-visibility: hidden;
        }

        .flip-box-front {
            background-color: #bbb;
            color: black;
        }

        .flip-box-back {
            background-color: orangered;
            color: white;
            transform: rotateY(180deg);
        }
    </style>
</head>

<body>
    <h1>ACTIVIDADES DISTRITO</h1>


    {# del proyecto4 templates/mostrar/verDepartamentos.html.twig #}
    <div class="distrito">
        <form name="formDistrito" method="POST" action="{{ path('actividadesDistrito') }}"> 
            <label for="distrito"></label>
            {# con el onchange manda el formulario automaticamente, es javascript #}
            <select name="distrito" id="distrito" onchange='formDistrito.submit()'>
                {% for d in distritos %}
                    {% if seleccionado is defined and d.distrito == seleccionado %}
                        <option value="{{d.distrito}}" selected>{{d.distrito}}</option>
                        {# en estos framework la forma de acceder a la propiedad es
                        poniendo el nombre de la propiedad, en realidad estas accediendo
                        a través de un get.
                        Si la varible es $num_cama y el get es getNumCama se pone numCama #}
                    {% else %}
                        <option value="{{d.distrito}}">{{d.distrito}}</option>
                    {% endif %}
                {% endfor %}
            </select>
        </form>
    </div>


    
    <div class="actividades">
        {% for actividad in actividades %}

        <div class="flip-box">
            <div class="flip-box-inner">
                <div class="flip-box-front">
                    <h2>{{actividad.nombre}}</h2>
                    <img src="" alt="">
                </div>
                <div class="flip-box-back">
                    <h2>{{actividad.nombre}}</h2>
                    <p class="descActividad">
                        {{actividad.descripcion}}
                    </p>
                </div>
            </div>
        </div>

        {% endfor %}

    </div>


</body>

</html>