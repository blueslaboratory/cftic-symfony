<!DOCTYPE html>
<html>

<head>
	<meta charset="UTF-8">
	<title>
		Calculadora
	</title>
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
		integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
	<link rel="stylesheet" href="css/styles.css">
</head>

<body>
	<div class="d-flex justify-content-center">
		<div class="card bg-light col-6 mt-4">
			<div class="card-body">
				<div class="container mt-4 mb-4">
					<h3 class="mb-4">Listado de empleados despedidos</h3>
					{% if arrayDespedidos is not empty %}
					{{dump(arrayDespedidos)}}
					{% for desp in arrayDespedidos %}
					<h4 class="mb-4">{{desp.apellido|capitalize}}</h4>
					<div class="card mb-4">
						<ul class="list-group list-group-flush">
							<li class="list-group-item">{{desp.oficio}}</li>
							<li class="list-group-item">
								<a href="{{ path('readmitir', {id:desp.id}) }}" class="badge badge-primary">Quitar de sesión</a>
							</li>
					</div>
					{% endfor %}
					{% else %}
					<p id="contador">Eres buen jefe, todavía no has despedido a nadie :-)</p>
					{% endif %}

					<p id="contador">Suma de salarios que me ahorro: {{ sumaSalarios }} €</p>
					<a href="{{ path('inicio') }}" class="btn btn-info" role="button">&lt; Volver</a>
					<a href="{{ path('limpiar') }}" class="btn btn-info" role="button">Readmitir a todos</a>
				</div>
			</div>
		</div>
	</div>
	<script src="/jquery-3.2.1.min.js" type="text/javascript"></script>
	<script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js"
		integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q"
		crossorigin="anonymous"></script>
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js"
		integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl"
		crossorigin="anonymous"></script>
</body>